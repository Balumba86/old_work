import { Layout } from '../views'

const ContactsPage = () => {
  return (
    <Layout>
      <main>
        <section>

        </section>
      </main>
    </Layout>
  )
}

export default ContactsPage